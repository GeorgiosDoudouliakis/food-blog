import { GoTopVisibilityDirective } from './go-top-visibility.directive';

describe('GoTopVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new GoTopVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
