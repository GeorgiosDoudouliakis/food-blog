import { ImgDownloadDirective } from './img-download.directive';

describe('ImgDownloadDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgDownloadDirective();
    expect(directive).toBeTruthy();
  });
});
